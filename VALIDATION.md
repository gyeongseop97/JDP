# v4 검증 기록 · 2026-09-23

- 엔진의 36개 규칙 검증과 3,000회 혼합 거래 시뮬레이션 통과.
- 구버전(v3) 실제 저장을 브라우저에서 불러와 금고 4,321G, 재고, 수집록과 진행 중 거래가 v4로 보존됨을 확인.
- 27단계·7개 장 체험 튜토리얼의 실제 버튼 조작을 확인. 연습 중 원래 저장이 바뀌지 않고, 종료·새로고침 뒤 복구됨을 확인.
- 브라우저 화면 320×568, 390×844, 844×390, 1365×900에서 레이아웃과 가로 넘침 점검. 실제 휴대폰 기기의 터치·키보드 검증은 수행하지 않음.
- 실행 파일 26개가 /JDP/ 하위 주소에서 HTTP 200으로 열리고 원본과 일치. 이미지 13개 디코딩 통과.
- 최초 시작 화면은 title.webp만 요청하고, 상점·손님·물건 그림은 게임을 시작한 뒤 필요에 따라 불러옴.
- 최종 브라우저 실행 오류 없음. JavaScript 전체 문법 검사 통과.

이 검증은 기능과 상태 보존을 확인한 것이며, 장기간 플레이의 재미나 모든 실기기 호환성을 보장하는 테스트는 아닙니다.


# v4.1 대화 화면 검증

- 등장인물 아래에 현재 대사와 선택지가 함께 있는지, 기존 별도 대화 영역이 없는지 확인.
- 320px·390px 모바일 폭과 844px 가로 화면, 1365px 데스크톱 화면 점검. 가로 넘침 없음.
- 생성된 제목 로고와 대화창 프레임의 정상 표시, 한글 표기, 실제 모바일 화면 가독성을 확인.
- 전체 대화의 내부 스크롤과 닫기, Esc 닫기 뒤 원래 버튼으로 초점 복귀를 확인.
- 전체 대화를 열고 스크롤하고 닫아도 게임 상태 JSON이 바뀌지 않음을 확인.
- origin → detail → challenge → 250G 제안 → 매입을 실제 화면에서 진행. 매입 뒤 선택지는 숨겨지고 대화 기록은 계속 열림.
- 최신 튜토리얼 첫 단계에서 출처 질문·전체 대화 열기·닫기 세 목표를 실제로 해야 완료됨을 확인.
- 현재 방문의 32개 대화 제한을 제거한 회귀 검증 추가. 48개 이상 기록의 처음 문장·전체 순서·저장 복구·다음 손님 초기화를 포함한 37개 검증과 3,000회 혼합 거래 통과.
- 실행 자산 29개가 /JDP/ 하위 주소에서 HTTP 200 및 원본 바이트 일치. HTML ID 77개 중 중복 없음. 전체 JavaScript 문법 검사 통과.

실제 휴대폰 기기 테스트가 아닌 브라우저 화면 크기와 입력·스크롤 검증입니다.
